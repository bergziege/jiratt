<UserControl x:Class="Jiratt.UI.Modules.JiraModule.SubModules.TaskSearch.TaskSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mvvm="http://prismlibrary.com/"
             xmlns:designViewModels1="clr-namespace:Jiratt.UI.Modules.JiraModule.SubModules.TaskSearch.DesignViewModels"
             xmlns:taskSearch="clr-namespace:Jiratt.UI.Modules.JiraModule.SubModules.TaskSearch"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             mvvm:ViewModelLocator.AutoWireViewModel="True">
    <d:DesignProperties.DataContext>
        <designViewModels1:TaskSearchDesignViewModel />
    </d:DesignProperties.DataContext>
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
        
        <ComboBox ItemsSource="{Binding Path=AvailableProjects}" SelectedItem="{Binding Path=SelectedProject}" Grid.IsSharedSizeScope="True" Margin="2" MinWidth="50">
            <ComboBox.ItemTemplate>
                <DataTemplate DataType="taskSearch:IJiraProjectViewModel">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition SharedSizeGroup="ProjectKey"/>
                            <ColumnDefinition />
                            <ColumnDefinition SharedSizeGroup="ProjectName"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Text="{Binding Path=Key}" Padding="0" Margin="0"/>
                        <Separator Width="10" Visibility="Hidden" Grid.Column="1" />
                        <TextBlock Text="{Binding Path=Name}" Grid.Column="2"/>
                    </Grid>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <TextBox Grid.Row="1" MinWidth="50" Text="{Binding Path=IssueNumber, UpdateSourceTrigger=PropertyChanged}" Margin="2"/>
        <ListBox Grid.Row="2" ItemsSource="{Binding  Path=AvailableIssues}" Grid.Column="0" Grid.IsSharedSizeScope="True" SelectedItem="{Binding Path=SelectedIssue}">
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="'in arbeit' - Kommitkommentar" Command="{Binding Path=CreateCommitCommentCommand}" CommandParameter="in arbeit" />
                    <MenuItem Header="'fertig' - Kommitkommentar" Command="{Binding Path=CreateCommitCommentCommand}" CommandParameter="fertig"/>
                    <MenuItem Header="'fixes' - Kommitkommentar" Command="{Binding Path=CreateCommitCommentCommand}" CommandParameter="fixes" />
                </ContextMenu>
            </ListBox.ContextMenu>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="taskSearch:IJiraIssueViewModel">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition SharedSizeGroup="IssueType"></ColumnDefinition>
                            <ColumnDefinition SharedSizeGroup="IssueKey"></ColumnDefinition>
                            <ColumnDefinition SharedSizeGroup="IssueSummary"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Text="{Binding Path=TypeName}" Margin="2"/>
                        <TextBlock Grid.Column="1" Text="{Binding Path=Key}" Margin="2"/>
                        <TextBlock Grid.Column="2" Text="{Binding Path=Summary}" Margin="2" MaxWidth="200"/>

                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Grid.Row="3" Grid.Column="0" Content="Details anzeigen" Command="{Binding Path=ShowDetailsCommand}" Margin="5"/>
    </Grid>
</UserControl>
