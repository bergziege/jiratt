<UserControl x:Class="Jiratt.UI.Modules.TaskDetails.CommitComment.CommitCommentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Button Style="{StaticResource CommonTransparentButtonStyle}"
                HorizontalContentAlignment="Left"
                Command="{Binding Path=CopyCommitCommentToClipboardCommand}">

            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="ContentCopy" VerticalAlignment="Center"
                                         HorizontalAlignment="Center" />
                <TextBlock Text="Kommitkommentar" VerticalAlignment="Center" Margin="5,0,0,0" />
            </StackPanel>
        </Button>
    </Grid>
</UserControl>